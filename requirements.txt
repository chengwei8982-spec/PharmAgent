# Lightweight/common dependencies used across scripts.
# Recommended: create the conda env via environment.yml first (RDKit, SciPy stack),
# then pip-install this file for Python-only packages.

numpy
pandas
scipy
scikit-learn
matplotlib
seaborn
tqdm
networkx
requests
pydantic
lmdb
python-Levenshtein
pandas-flavor
transformers
ogb
tdc
plip
# Server (pin to pydantic v1 for compatibility)
pydantic<2
fastapi<0.100
uvicorn[standard]

# Heavy / CUDA-sensitive deps (install separately following official instructions):
# torch
# dgl
# torch-geometric
# dgllife
